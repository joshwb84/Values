<% provide(:title, 'Account') %>

<% @choices.each do |choice| %>

<h1><%= choice.value.name %></h1>
<em><p><%= choice.value.definition %></p></em>
 
<% end %>

<hr class="">
    <div class="container target">
        
            </div>
            <br>
                <div class="row">
                    <div class="col-sm-3">
                        <!--left col-->
                        <ul class="list-group">
                            <li class="list-group-item text-right">
                                <span class="pull-left">
                                    <strong><%= @user.name %></strong>
                                </span><div class="btn-group"><%= link_to "Edit my details", edit_user_path(current_user), class: "btn  btn-default btn-xs"%>
                            </li>


                            <li class="list-group-item text-right">
                                <span class="pull-left">
                                    <strong>Date joined</strong>
                                </span><%= @user.created_at.strftime("%B %Y") %>
                            </li>
                            <li class="list-group-item text-right">
                                <span class="pull-left">
                                    <strong>Email</strong>
                                </span><%= @user.email %>
                            </li>
                            <li class="list-group-item text-right">
                                <span class="pull-left">
                                    <strong>Values</strong>
                                </span><%= @choices.count %>
                            </li>
                        </ul>
                        <div class="panel panel-default">
                            <div class="panel-heading">Social profiles</div>
                            <div class="panel-body">
                                <a href="<%= @user.facebook %>">
                                    <i class="fa fa-facebook fa-2x"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                    <!--/col-3-->
                    <div class="col-sm-9" contenteditable="false" style="">
                        <div class="panel panel-default">
                            <div class="panel-heading">
                            </div>
                            <div class="panel-body">
                            </div>
                        </div>
                    </div>
                </div>
        
    </div>    

